import Vue from 'vue'
import App from './App.vue'
// const shake = require('../node_modules/shake.js/shake')
// Vue.use(shake);
new Vue({
  el: '#app',
  render: h => h(App)
})


// Mỗi thành phần trong Web tương ứng mỗi Component -> Tương ứng với 1 file *.vue